<AuPanel class="au-u-margin-bottom-tiny au-u-flex-self-stretch" as |Section|>
  <Section>
    <AuToolbar>
      <div class="au-u-flex au-u-flex--column">
        <AuLabel>
          Titel
        </AuLabel>
        <AuInput
          value={{@requirement.title}}
          {{on "input" (with-value (fn this.updateProperty @requirement "title"))}}
        />
      </div>
      <div>
        <AuButtonGroup>
          <AuButton
            @skin="naked"
            @icon="bin"
            @alert={{true}}
            {{on "click" @remove}}
          >
            Verwijder
          </AuButton>
        </AuButtonGroup>
      </div>
    </AuToolbar>
  </Section>
  <Section>
    <AuLabel>
      Omschrijving
    </AuLabel>
    <AuTextarea
      class="au-u-1-1"
      value={{@requirement.description}}
      {{on "input" (with-value (fn this.updateProperty @requirement "description"))}}
    />
  </Section>
</AuPanel>